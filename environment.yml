# conda env create --file environment.yml
name: regresion_lineal
channels:
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - fs
  - jupyter
  - jupyterlab
  - pathlib
  - pip
  - pyprojroot
  - python=3.12
  - pip:
    - pyhere
    